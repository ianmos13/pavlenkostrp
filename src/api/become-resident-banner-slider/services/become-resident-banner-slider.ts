/**
 * become-resident-banner-slider service
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreService('api::become-resident-banner-slider.become-resident-banner-slider');
