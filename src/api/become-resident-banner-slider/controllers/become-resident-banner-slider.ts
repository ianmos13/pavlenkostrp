/**
 * become-resident-banner-slider controller
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreController('api::become-resident-banner-slider.become-resident-banner-slider');
