/**
 * running-line-become-resident controller
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreController('api::running-line-become-resident.running-line-become-resident');
