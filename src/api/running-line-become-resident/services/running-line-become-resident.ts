/**
 * running-line-become-resident service
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreService('api::running-line-become-resident.running-line-become-resident');
