/**
 * running-line-become-resident router
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreRouter('api::running-line-become-resident.running-line-become-resident');
